# tenant-operator mints this into .Values.forwarder.natsSecretName, scoped to
# the JetStream API and the dead letter subjects. Nothing is templated here, so
# no key material lands in the Helm release.
