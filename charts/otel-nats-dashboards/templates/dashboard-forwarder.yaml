apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-nats-dashboard-forwarder
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{ .Values.sidecarLabel.key }}: {{ .Values.sidecarLabel.value | quote }}
  annotations:
    grafana_folder: {{ .Values.folder | quote }}
data:
  forwarder.json: |-
{{ .Files.Get "files/forwarder.json" | indent 4 }}
